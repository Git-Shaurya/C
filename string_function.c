#include<stdio.h>
int main () {

    char *canchange = "hello world";
    puts(canchange);
    canchange  = "Hello";
    puts(canchange); // here we can modify the value of the string.


    char cannot change = "Shaurya Patil"
    puts(canchange);
    canchange("Shaurya");// this wont work because we cannot change the variable later.
      

    

}