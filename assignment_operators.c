#include<stdio.h>

int main() {

    int a = 4;
    int b = 5;

    a += b ; // this is written in replacement of a = a + b

    printf("%d\n", a);

    // same goes for multiplcation , difference and division. *=, -=, /=, %=.

}